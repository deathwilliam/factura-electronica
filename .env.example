# Database (Railway PostgreSQL - se configura automáticamente)
DATABASE_URL="postgresql://user:password@host:5432/railway"

# Auth
AUTH_SECRET="generate-a-secret-with-openssl-rand-base64-32"

# DTE Environment (00=Pruebas, 01=Producción)
DTE_AMBIENTE="00"

# App URL (Railway lo configura automáticamente)
NEXTAUTH_URL="https://your-app.railway.app"
